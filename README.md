# React DUG talk, 15 July 2015

You find the slides in `slides/index.html`.

To run the demos, make sure nodejs is installed:

```
npm install
```

Start demo server

```
npm start
```

and visit http://localhost:8080